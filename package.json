{
  "name": "smartroute-srmist",
  "version": "6.0.0",
  "private": true,
  "type": "module",
  "description": "SmartRoute SRMIST — Multi-agent autonomous AI travel planner (React + Cloudflare Pages Functions)",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "preview": "wrangler pages dev dist --ip 0.0.0.0 --port 3000",
    "deploy": "npm run build && wrangler pages deploy dist --project-name smartroute-srmist",
    "deploy:prod": "npm run build && wrangler pages deploy dist --project-name smartroute-srmist --branch main",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "framer-motion": "^12.0.0",
    "leaflet": "^1.9.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.22.0",
    "recharts": "^2.12.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.4.1",
    "vite": "^6.3.5",
    "wrangler": "^4.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
